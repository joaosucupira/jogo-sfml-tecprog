// #include <SFML/Graphics.hpp>
// #include <iostream>
#include "Principal.hpp"

int main() {
    Principal principal;

    return 0;
}
// int main() {

    // sf::RenderWindow window(sf::VideoMode(400, 400), "Jogo");
    // while (window.isOpen()) {

    //     sf::Event event;
    //     while (window.pollEvent(event)) {
    //         if (event.type == sf::Event::Closed)
    //             window.close();
    //     }
    //     window.clear();
    //     window.display();
    // }


//     return 0;
// }