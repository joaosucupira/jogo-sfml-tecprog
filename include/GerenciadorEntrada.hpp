#pragma once
#include <list>
using namespace std::list;

#include "stdfax.h"

class GerenciadorEntrada
{   
private:
    /* data */
public:
    GerenciadorEntrada(/* args */);
    ~GerenciadorEntrada();
};

GerenciadorEntrada::GerenciadorEntrada(/* args */)
{
}

GerenciadorEntrada::~GerenciadorEntrada()
{
}
